const nombre = "Deedpool"
const real = "Wade Winston"
const normal = nombre + ' ' + real;

const template = `${nombre} ${real}`

console.log(normal, template)

const html = `
    <h1>Hola</h1>
    <p>Mundo</p>
    `;

console.log(html)